datasource db {
    provider = "postgresql"
    url      = env("DATABASE_URL")
}

generator client {
    provider = "prisma-client-js"
}

model FavouriteSongs {
    id     String    @id
    name   String
    artist String
    album  String
    userId String
}